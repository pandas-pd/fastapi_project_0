// used to pair raw data with keys with enum data

export function parseUserRoles (userRoles : any, RolesEnum : any) : object {

    let data : object = {};

    console.log(userRoles);
    console.log(RolesEnum);

    return data;
}